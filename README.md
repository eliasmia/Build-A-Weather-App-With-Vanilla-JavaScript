# Build-A-Weather-App-With-Vanilla-JavaScript
A responsive Weather App built with Vanilla JavaScript that uses a weather API to fetch and display real-time weather data. Features include city search, dynamic UI updates, and clean JavaScript code without any external libraries.
